<h1>Books</h1>
  <% @books.each do |book| %>
    <div>
      <div><%= link_to book_path(book.id) %></div>
      <p><%= book.body %></p>
    </div>
  <% end %>
    
<h1>New Book</h1>
<%= form_with model: @book, url:'/books' do |f| %>
  <p>Titel</p>
    <%= f.text_field :title %>
  <p>Body</p>
    <%= f.text_field :body %>
  <%= f.submit 'Create Book' %>
<% end %>
